= simple_form_for [:admin, @club, @court], :html => { :class => 'form-horizontal' } do |f|
  = f.association :sport


  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), admin_club_courts_path(@court.club.id), :class => 'btn'
